# PAM limits for low-latency audio (applied to members of the 'audio' group).
# Allows PipeWire, JACK, and other audio daemons to use real-time scheduling
# and lock memory pages, which are both required for xrun-free operation.
@audio - rtprio  95
@audio - memlock unlimited
@audio - nice    -19
