# Show the GRUB menu with a 4-second timeout when kernel-audio-tuned is
# installed, so the user can select the audio-tuned boot entry.
# GRUB_TIMEOUT_STYLE=menu overrides menu_auto_hide on systems where it is set.
GRUB_TIMEOUT=4
GRUB_TIMEOUT_STYLE=menu
